import React, { FC } from 'react';
import { Title } from '@mantine/core';

type Props = {
  
};

const HeightPage: FC<Props> = (props: Props) => {
  const {  } = props;

  return (
    <div>
      <Title order={1}>Height</Title>
      <Title order={2}>Percentage</Title>
    </div>
  );
};

export default HeightPage;
